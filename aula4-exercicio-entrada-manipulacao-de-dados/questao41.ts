// 41. Solicite ao usuário para digitar um número decimal e arredonde-o para o inteiro mais próximo.
let nume = Number(prompt(`Digite um número decimal: `));
let numeroArredonda = Math.round(nume);
alert(`O número arredondado é ${numeroArredonda}`);