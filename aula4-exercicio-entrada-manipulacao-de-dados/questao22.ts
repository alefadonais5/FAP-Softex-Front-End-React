// 22. Receba a base e a altura de um retângulo digitados pelo usuário. Em seguida, calcule a sua área e exiba o resultado.
let base = Number(prompt(`Qual a base do retângulo? `));
let altura = Number(prompt(`Qual a altura do retângulo`));

let area = base * altura;

alert(`A áre do retângulo é ${area}`);